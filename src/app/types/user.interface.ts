export interface User {
  firstName: string;
  lastName: string;
  email: string;
  birthDate: string,
  password?: string;
}

export interface userRequest {
  firstName: string;
  lastName: string;
  email: string;
  birthDate: string,
  password?: string;
}
